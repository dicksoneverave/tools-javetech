# Secrets for Supabase Edge Functions (server-side only — never put in VITE_ vars)
# Set these via: supabase secrets set KEY=value

# Auto-provided by Supabase runtime — no action needed:
# SUPABASE_URL=https://bfquozutpflvlfyddbdg.supabase.co
# SUPABASE_SERVICE_ROLE_KEY=<your service role key>

# You must set this yourself:
# PADDLE_WEBHOOK_SECRET=pdl_ntfset_...
